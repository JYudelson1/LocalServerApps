# Example secrets file — copy to `.env` and fill in real values.
# `.env` is gitignored; `.env.example` is tracked so we can see what's expected.

# MTA Bus API key — get one at https://register.developer.obanyc.com/
MTA_BUS_API_KEY=
